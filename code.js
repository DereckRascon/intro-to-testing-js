// helloWorld function 
function helloWorld() {
    return "Hello, World!";
}

function sayHello(input) {
    if (input === 'Jane') {
        return 'Hello ' + input + '!';
    } else if (input === 'Pat'){
        return 'Hello ' + input + '!';
    } else if (input === 'Alex'){
        return 'Hello ' + input + '!';
    } else {
        return ''
    }
}